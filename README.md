#micro-express
Quick start repo for building microservices with ES6, Node, Express and Docker.

##Pre-requisites
- Docker
- Docker Compose

##Build
To build, simply run:
```
docker-compose build
```

##Run
To run, execute:
```
docker-compose up
```
